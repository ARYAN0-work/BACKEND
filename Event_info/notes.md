## what it is => handling events

- ex- user clciks a btn and we handle it in our program

- as our ptogram excutes at least in part it is controlled by events of course depending on a program but its safe to say that in browesr app those eveents are mostly external now that style of programming is called event driven programming and effectivlty its just that a style in which the flow of the program is at least in part determined by the eveents that occurr as the program  excutes now it is easier to imagaine when you have a graphical interface just like the btn and links example i just mention but what about server side is it also possible and of cours the ans is yes in fact event drivven programming is used heavily in node.js and in the following videos we'll see some ex:



📝 What your notes say (in simple terms)
🔹 Title:

“what it is => handling events”

🔹 Main idea:
Example:
👉 User clicks a button → your program handles it
🔹 Explanation:
Your program doesn’t just run step-by-step blindly
It is controlled by events (like clicks, requests, etc.)
In browser apps:
Events mostly come from users (external)
Like button clicks, typing, scrolling
🔹 Concept name:

👉 This style is called Event-Driven Programming

🔹 Meaning of that:
The flow of your program is decided by events

Instead of:

Step 1 → Step 2 → Step 3

It works like:

Event happens → Code runs
Event happens → Code runs
🔹 Important part you wrote:
This is not just browser stuff
✅ It is also used on the server side

👉 Especially in Node.js, event-driven programming is used heavily

🔹 Final line:

You wrote that:

In upcoming videos, you’ll see examples of this

💡 In one line (your note summary)

👉 Programs react to events instead of running in a fixed order — that’s event-driven programming.